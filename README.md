# Redsocial

1. Renombrar el archivo ```.env.template``` por ```.env```
2. Agregar variables de entorno de desplieguee (default local)


## Compilar proyecto
```
go build -o server.exe ./cmd/api/ && ./server.exe
```

## Go Version
```
1.25.1
```